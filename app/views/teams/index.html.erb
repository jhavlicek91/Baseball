<div class="container">
	<div class="row">
		<div class="col-md-2"><%= image_tag @image_url %></div>
		<div class="col-md-3">
			<h2><%= @team.city.name + " " + @team.name %></h2>
			<div><%= @team.division.conference.abbreviation + " " + @team.division.name%></div>
		</div>
	</div>
</div>